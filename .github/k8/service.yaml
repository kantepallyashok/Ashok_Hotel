apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000  # Match the port to what your app exposes
  type: LoadBalancer  # This creates an external load balancer (if using AWS)
